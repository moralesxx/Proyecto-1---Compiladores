program {
    // ERROR SEMÁNTICO 1
    // La gramática acepta este código (léxico y sintaxis OK).
    // El SemanticVisitor detecta:
    //
    // Error 1 - visitVariable: 'limite' no fue declarada en ningún scope.
    //           Se usa en la condición del while pero no tiene declaracion previa.
    //
    // Error 2 - visitVariable: 'acumulador' no fue declarada.
    //           Se usa en print() sin haber sido declarada antes.
    //
    // El pipeline se detiene en FASE 3 - Semántico

    int contador = 0;
    int paso = 3;

    while (contador < limite) {
        contador = contador + paso;
    }

    print("Contador:");
    print(contador);

    print("Acumulador:");
    print(acumulador);
}
