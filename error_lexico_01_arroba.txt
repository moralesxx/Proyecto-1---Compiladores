program {
    // ERROR LÉXICO 1
    // El símbolo '@' NO está definido en ninguna regla léxica
    // de la gramática Expresiones.g
    // El lexer lanzará: token recognition error at: '@'
    // El pipeline se detiene en FASE 1 - Léxico

    int x = 10;
    int y = @25;
    int z = x + y;
    print(z);
}
