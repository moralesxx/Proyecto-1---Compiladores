program {
    // ERROR SINTÁCTICO 1
    // Viola la regla: declaracion : TIPO ID (ASIGNACION expr)?
    // Falta el punto y coma -> PUNTO_COMA requerido en #InstrDecl
    // Viola la regla: WHILE PAR_IZQ condicion PAR_DER bloque
    // Falta PAR_DER ')' en el while
    // El pipeline se detiene en FASE 2 - Sintáctico

    int x = 5
    int y = 10;

    while (x > 0 {
        x = x - 1;
        y = y + 1;
    }

    print(x);
    print(y);
}
